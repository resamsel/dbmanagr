article
→ user_id → user.id
  ↑ article (user_id → user.id)
  ↑ user_address (user_id → user.id)
    → user_id → user.id
    → address_id → address.id
  ↑ blog_user (user_id → user.id)
    → blog_id → blog.id
    → user_id → user.id
