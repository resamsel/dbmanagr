language: python
python:
  - "2.7"
# command to install dependencies
install:
  - python setup.py bdist_egg
  - python setup.py install
# command to run tests
script:
  - python setup.py nosetests --with-coverage --cover-package=dbnav
